{
  "name": "campaign-planner",
  "version": "1.0.5",
  "private": true,
  "type": "module",
  "description": "Drupal 11 add-on: campaign planner with personas, messages, recipes, and AI assist. Vanilla JS/CSS, concatenated build, served via jsDelivr.",
  "scripts": {
    "build": "node scripts/build.mjs"
  }
}
